### Run CSS

```bash
pnpm run build
```

### Run Tailwind

```bash
pnpm run dev
```


### Deploy

This project is deployed on Github pages.

It will serve the ``dist`` dir - so do NOT add the ``dist`` to gitignore :)